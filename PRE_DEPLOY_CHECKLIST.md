# ✅ Чеклист перед деплоєм на Vercel

## 🔧 Environment Variables (Vercel Dashboard → Settings → Environment Variables)

### ⚠️ ОБОВ'ЯЗКОВІ змінні (додати ОБОВ'ЯЗКОВО):

#### 1. DATABASE_URL
```
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
```
**Де взяти:** З вашого провайдера БД (Neon, Supabase, тощо)
**Важливо:** Має містити `?sslmode=require` в кінці

#### 2. JWT_SECRET
```
JWT_SECRET=ваш_випадковий_рядок_мінімум_32_символи
```
**Як згенерувати:**
```bash
openssl rand -base64 32
```
або
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### 3. JWT_REFRESH_SECRET
```
JWT_REFRESH_SECRET=інший_випадковий_рядок_мінімум_32_символи
```
**Важливо:** Має бути ІНШИЙ, ніж JWT_SECRET!
**Як згенерувати:** Запустіть команду генерації ще раз

#### 4. NODE_ENV
```
NODE_ENV=production
```

---

### 📝 ОПЦІОНАЛЬНІ змінні (додати за потреби):

#### 5. CORS_ORIGIN (якщо клієнт на окремому домені)
```
CORS_ORIGIN=https://your-client-app.vercel.app
```
**Примітка:** Якщо клієнт і сервер на одному домені - не додавайте

#### 6. BASE_URL (додати ПІСЛЯ першого деплою)
```
BASE_URL=https://your-server-app.vercel.app
```
**Важливо:** Додайте після першого деплою, коли знаєте фінальний URL

#### 7. JWT_EXPIRES_IN (якщо хочете змінити за замовчуванням)
```
JWT_EXPIRES_IN=15m
```

#### 8. JWT_REFRESH_EXPIRES_IN (якщо хочете змінити за замовчуванням)
```
JWT_REFRESH_EXPIRES_IN=7d
```

#### 9. STORAGE_TYPE (якщо використовуєте локальне сховище)
```
STORAGE_TYPE=local
STORAGE_PATH=./uploads
```

---

## 📋 Чеклист перед деплоєм:

### Перед додаванням Environment Variables:
- [ ] Маєте DATABASE_URL з провайдера БД
- [ ] Згенеровано JWT_SECRET (мінімум 32 символи)
- [ ] Згенеровано JWT_REFRESH_SECRET (інший, мінімум 32 символи)

### В Vercel Dashboard:
- [ ] Додано `DATABASE_URL`
- [ ] Додано `JWT_SECRET`
- [ ] Додано `JWT_REFRESH_SECRET`
- [ ] Додано `NODE_ENV=production`
- [ ] (Опціонально) Додано `CORS_ORIGIN` якщо клієнт на окремому домені

### Після першого деплою:
- [ ] Додано `BASE_URL` з URL вашого деплою
- [ ] Перевірено `/health` endpoint
- [ ] Перевірено `/api/auth/login` endpoint

---

## 🗃️ Міграції бази даних

**ВАЖЛИВО:** Перед або після деплою потрібно застосувати міграції:

```bash
# З локального комп'ютера (використовуючи production DATABASE_URL)
cd server
npx prisma migrate deploy
```

Або встановіть DATABASE_URL в .env локально і запустіть:
```bash
npx prisma migrate deploy
```

---

## 🚨 Важливі примітки:

1. **JWT Secrets:** НЕ використовуйте тестові значення в production!
2. **DATABASE_URL:** Переконайтеся, що connection string правильний і включає `?sslmode=require`
3. **CORS_ORIGIN:** Якщо не впевнені - не додавайте, Vercel автоматично обробляє
4. **BASE_URL:** Додайте тільки після першого деплою, коли знаєте фінальний URL

---

## 📝 Приклад мінімального набору Environment Variables:

```
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
JWT_SECRET=abc123def456ghi789jkl012mno345pqr678stu901vwx234yz5678901234
JWT_REFRESH_SECRET=xyz789abc123def456ghi789jkl012mno345pqr678stu901vwx234
NODE_ENV=production
```

Цього достатньо для базового деплою!
